<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.doctor">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".update_patient"></activity>
        <activity android:name=".logindoc" />
        <activity android:name=".AddPrescriptionsActivity" />
        <activity android:name=".ViewVisitActivity" />
        <activity
            android:name=".PatientProfileActivity"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".AddVisitActivity" />
        <activity
            android:name=".updatepassword"
            android:parentActivityName=".Main2Activity" />
        <activity android:name=".forgotpass" />
        <activity android:name=".Main2Activity" />
        <activity android:name=".PatientRegisterFormActivity" />
        <activity android:name=".splashscreen"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".register" />
    </application>

</manifest>